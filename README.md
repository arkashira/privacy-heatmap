# Privacy Heatmap
A local data collection engine for capturing click, scroll, and mouse-move events.

## Usage
1. Run `python -m pytest` to run the tests.
2. Run `python src/privacy_heatmap.py` to run the main script.

## Requirements
* Python 3.8+
* No external dependencies required.
